export { claimToMsg, shouldWakeOnClaim, shouldWakeOnMsg } from '@room/shared'
// phase 2: switch runner wake/preemption semantics fully to message priorities.
export type { WakeDecision } from '@room/shared'
